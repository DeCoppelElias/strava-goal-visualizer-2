// Public-facing site configuration shared across pages.

/** Product name shown in the UI and legal copy. */
export const APP_NAME = 'Strava Goal Visualizer'

/** Operator of the service, shown in the privacy policy and terms. */
export const OPERATOR_NAME = 'Elias De Coppel'

/** Contact address shown in the privacy policy, terms, and other legal copy. */
export const SUPPORT_EMAIL = 'goalvisualizer.support@gmail.com'
