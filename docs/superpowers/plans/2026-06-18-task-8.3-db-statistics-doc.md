# TASK-8.3 — DB Statistics Doc Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give the operator a documented, repeatable way to read usage statistics from PostgreSQL — locally and against the deployed Fly database.

**Architecture:** A single canonical `scripts/stats.sql` holds the curated summary query. The reference doc (`docs/ops/db-statistics.md`) documents how to run that file locally (via `docker compose exec`) and in production (via `fly postgres connect`), plus a full query catalogue for ad-hoc questions. No `make` target — the commands are documented directly so they work identically in both environments without extra tooling.

**Tech Stack:** PostgreSQL 16, `psql`, Docker Compose, Fly.io CLI.

**Spec:** `docs/superpowers/specs/2026-06-18-observability-basics-design.md`

**One-commit rule:** Per `CLAUDE.md`, one backlog task = one commit. Build and verify both files, then make a **single commit** in the final task. Do not commit between Tasks 1–2.

> **Design note:** No `make` target (the user opted for documented commands only). The local compose `db` service publishes no host port and the app's `DATABASE_URL` uses the `postgresql+asyncpg://` driver suffix `psql` cannot parse, so connections route through the container locally (`docker compose exec`) and through the Fly CLI in production. `scripts/stats.sql` is the single shared artifact for both.

---

## File Structure

- **Create `scripts/stats.sql`** — the curated summary as one ordered result set (UNION ALL with a `metric`/`value` shape). Single source of truth, run by both the local and Fly commands.
- **Create `docs/ops/db-statistics.md`** — the reference doc: how to run the summary (local + Fly), connect instructions, full query catalogue, read-only role, caveat box.

---

## Task 1: Curated stats query (`scripts/stats.sql`)

**Files:**
- Create: `scripts/stats.sql`

- [ ] **Step 1: Create `scripts/stats.sql`**

```sql
-- Curated usage summary. Run locally or against prod (see docs/ops/db-statistics.md).
-- All values cast to text so they share one result set. Order is preserved.
SELECT 'Total users'              AS metric, count(*)::text AS value FROM users
UNION ALL
SELECT 'Signups (last 7 days)',   count(*)::text
  FROM users WHERE created_at > now() - interval '7 days'
UNION ALL
SELECT 'Users who have synced',   count(DISTINCT user_id)::text FROM activities
UNION ALL
SELECT 'Users with a goal set',   count(*)::text FROM goals
UNION ALL
SELECT 'Total activities',        count(*)::text FROM activities
UNION ALL
SELECT 'Total distance (km)',     coalesce(round(sum(distance_meters) / 1000, 1), 0)::text
  FROM activities
UNION ALL
SELECT 'Clubs tracked',           count(*)::text FROM clubs;
```

- [ ] **Step 2: Ensure the local DB exists with the current schema**

The compose `db` service has no published host port, so connect via the container. Bring the stack up so migrations run (the backend applies them on startup):

Run:
```bash
docker compose up -d
```
Expected: `db` and `backend` become healthy (`docker compose ps` shows `healthy`). The schema now exists.

- [ ] **Step 3: Run the query file against the local DB to verify it executes**

Run:
```bash
docker compose exec -T db psql -U postgres -d strava_dev -f - < scripts/stats.sql
```
Expected: a two-column table prints without error, e.g. (values will be `0` on an empty DB — that is a pass):
```
        metric         | value
-----------------------+-------
 Total users           | 0
 Signups (last 7 days) | 0
 Users who have synced | 0
 Users with a goal set | 0
 Total activities      | 0
 Total distance (km)   | 0
 Clubs tracked         | 0
```
If you see a SQL error (e.g. a wrong column name), fix `scripts/stats.sql` and re-run.

---

## Task 2: Reference doc (`docs/ops/db-statistics.md`)

**Files:**
- Create: `docs/ops/db-statistics.md`

- [ ] **Step 1: Create `docs/ops/db-statistics.md`**

```markdown
# Database Statistics — Querying Usage

How to read usage statistics for the Strava Goal Visualizer straight from
PostgreSQL, both locally and against the deployed Fly.io database. For a quick
glance, run the curated summary file (`scripts/stats.sql`); for anything ad-hoc,
use the query catalogue below.

> **Scope note:** These statistics use data already in the database. There is no
> `last_seen_at` column, so "active users" is approximated by *who has synced
> activities*, not login recency. Sessions are signed cookies (no server-side
> session store), so there is no "currently online" count — that state does not
> exist on the server.

---

## Quick glance: run the curated summary

`scripts/stats.sql` prints a one-table summary (total users, signups in the last
7 days, users who have synced, users with a goal, total activities, total
distance, clubs):

```
        metric         | value
-----------------------+-------
 Total users           |   342
 Signups (last 7 days) |    18
 Users who have synced |   310
 Users with a goal set |   276
 Total activities      | 48201
 Total distance (km)   | 312880.5
 Clubs tracked         |    27
```

**Local:**

```bash
docker compose exec -T db psql -U postgres -d strava_dev -f - < scripts/stats.sql
```

**Production (Fly.io):**

```bash
fly postgres connect -a <db-app> -d <db-name> < scripts/stats.sql
```

Replace `<db-app>` with your Postgres app name (`fly apps list`) and `<db-name>`
with the application database (e.g. `strava`). No tunnel or exposed port is
needed — `fly postgres connect` proxies through the Fly CLI.

> **Which Fly Postgres?** The command above is for **Fly Postgres** (the
> unmanaged app). If you provisioned **Managed Postgres**, use `fly mpg` instead
> — run `fly mpg --help` / `fly pg --help` to confirm the connect syntax for your
> setup. Either way, piping `scripts/stats.sql` into the resulting `psql` session
> works the same.

---

## Connecting (interactive sessions)

### Local (Docker Compose)

The compose `db` service does not publish a host port, so connect through the
container:

```bash
docker compose exec db psql -U postgres -d strava_dev
```

### Production (Fly.io)

The PostgreSQL port is never exposed publicly. Use the Fly CLI:

```bash
# Quick interactive session
fly postgres connect -a <db-app> -d <db-name>

# Or open a tunnel for a GUI client (e.g. TablePlus, DBeaver)
fly proxy 5432 -a <db-app>      # forwards localhost:5432 to the prod DB
# then connect your client to:
#   postgresql://postgres:<password>@localhost:5432/<db-name>
```

> Cross-reference: `fly logs -a <app>` streams the live plain-text logs (each line
> carries an `X-Request-ID`), useful for correlating anomalies you spot in the
> stats.

### Read-only role (recommended)

Run statistics through a role that cannot mutate data:

```sql
CREATE ROLE stats_reader WITH LOGIN PASSWORD 'choose-a-strong-password';
GRANT CONNECT ON DATABASE <db-name> TO stats_reader;
GRANT USAGE ON SCHEMA public TO stats_reader;
GRANT SELECT ON ALL TABLES IN SCHEMA public TO stats_reader;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT ON TABLES TO stats_reader;
```

Then connect with `stats_reader` instead of the app's role.

---

## Query catalogue

### Users

```sql
-- Total registered users
SELECT count(*) FROM users;

-- Signups per week (most recent first)
SELECT date_trunc('week', created_at) AS week, count(*)
FROM users GROUP BY 1 ORDER BY 1 DESC;

-- Most recent signups
SELECT id, display_name, created_at
FROM users ORDER BY created_at DESC LIMIT 20;
```

### Engagement

```sql
-- Users who have synced at least one activity
SELECT count(DISTINCT user_id) FROM activities;

-- Users with a yearly goal set
SELECT count(*) FROM goals;

-- Activities per user (most active first)
SELECT u.id, u.display_name, count(a.id) AS activities
FROM users u LEFT JOIN activities a ON a.user_id = u.id
GROUP BY u.id, u.display_name
ORDER BY activities DESC;
```

### Clubs

```sql
-- Clubs tracked
SELECT count(*) FROM clubs;

-- Members per club (largest first)
SELECT c.id, c.name, count(m.user_id) AS members
FROM clubs c LEFT JOIN club_memberships m ON m.club_id = c.id
GROUP BY c.id, c.name
ORDER BY members DESC;
```

### Content volume

```sql
-- Total activities and combined distance (km)
SELECT count(*) AS activities,
       round(sum(distance_meters) / 1000, 1) AS total_km
FROM activities;

-- Data date range
SELECT min(start_date) AS earliest, max(start_date) AS latest
FROM activities;
```

> All stored activities are runs (`sport_type = 'Run'` is enforced at ingest),
> so no sport filter is needed.
```

- [ ] **Step 2: Verify every catalogue query runs against the local DB**

For each query block in the doc, paste it into a session and confirm it executes
without error (empty results / zeros are fine on an empty DB):

Run:
```bash
docker compose exec db psql -U postgres -d strava_dev
```
Then paste each `SELECT` from the catalogue. Expected: each returns a result set
with the described columns and no error. Quit with `\q`.

---

## Task 3: Full verification + single commit

**Files:**
- Both from Tasks 1–2.

- [ ] **Step 1: Final verification of the deliverable**

Run:
```bash
docker compose exec -T db psql -U postgres -d strava_dev -f - < scripts/stats.sql
```
Expected: the summary table prints with no error.

- [ ] **Step 2: Confirm no stray changes**

Run:
```bash
git status --short
```
Expected: only `scripts/stats.sql` and `docs/ops/db-statistics.md` appear as
additions (plus any pre-existing untracked files you did not touch — leave those
alone).

- [ ] **Step 3: Mark TASK-8.3 complete in the backlog**

In `docs/epics/backlog.md`, change the heading `#### TASK-8.3` to
`#### TASK-8.3 ✅`.

- [ ] **Step 4: Commit (single commit for the whole task)**

Run:
```bash
git add scripts/stats.sql docs/ops/db-statistics.md docs/epics/backlog.md
git commit -m "docs(ops): document DB statistics queries

Add docs/ops/db-statistics.md (local + Fly run/connect instructions,
query catalogue, read-only role, limitations) and scripts/stats.sql
curated summary. Implements TASK-8.3.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```
Expected: pre-commit hooks pass; the commit is created.

---

## Self-Review

**Spec coverage:**
- Run instructions for the curated summary (local + Fly) → Task 2 "Quick glance".
- Interactive connect instructions (local + Fly, tunnel) → Task 2 "Connecting".
- Read-only role recommendation → Task 2 "Read-only role".
- Query catalogue (users / engagement / clubs / content volume) → Task 2 catalogue.
- Caveat box (no `last_seen_at`, no session store) → Task 2 scope note.
- `scripts/stats.sql` curated subset → Task 1.
- `fly logs` cross-reference → Task 2 "Production (Fly.io)".
- No `make` target (per user decision) — reflected throughout; no `Makefile` edits.
- Single commit per one-task-one-commit rule → Task 3.

**Placeholder scan:** No TBD/TODO. `<db-app>`, `<db-name>`, `<password>` are
deliberate, documented user-supplied values in the *production* commands only.

**Consistency:** `scripts/stats.sql` column names (`metric`, `value`) and the
`docker compose exec -T db psql -U postgres -d strava_dev` / `fly postgres
connect ... < scripts/stats.sql` invocations are identical across Tasks 1–3.
Database/user names (`postgres` / `strava_dev`) match `docker-compose.yml`.
