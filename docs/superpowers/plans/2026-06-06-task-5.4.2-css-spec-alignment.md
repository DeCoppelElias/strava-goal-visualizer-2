# TASK-5.4.2 — Align Dashboard CSS to Style Spec

**Goal:** Fix two values in `index.css` that deviate from `docs/design/style.md`:
- `.dashboard-page` gap: 24px → 48px (spec: "48px between major sections")
- `.page-title` font-size: 22px → 24px (spec type scale: "Page title — 24px")

---

## File Map

| File | Action |
|------|--------|
| `frontend/src/index.css` | Modify — two one-line changes |

---

### Step 1 — Fix `.dashboard-page` gap

```css
/* Before */
.dashboard-page {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

/* After */
.dashboard-page {
  display: flex;
  flex-direction: column;
  gap: 48px;
}
```

---

### Step 2 — Fix `.page-title` font-size

```css
/* Before */
.page-title {
  font-size: 22px;
  ...
}

/* After */
.page-title {
  font-size: 24px;
  ...
}
```

---

### Step 3 — Verify

- Dashboard: sections (Sync card, stats row, chart card, goal card) have noticeably more vertical breathing room.
- Page title ("Dashboard") renders at 24px.
- No layout breakage at narrow widths.

---

### Step 4 — Commit

```bash
git add frontend/src/index.css
git commit -m "fix(frontend): align section gap (48px) and page title (24px) to style spec"
```
